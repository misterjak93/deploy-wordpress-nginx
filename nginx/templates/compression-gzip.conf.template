# --- gzip: il minimo comune denominatore, lo capiscono tutti ---
gzip                on;
gzip_vary           on;
gzip_proxied        any;
gzip_comp_level     ${GZIP_LEVEL};
gzip_min_length     ${COMPRESSION_MIN_LENGTH};
gzip_disable        "msie6";
# Se un plugin di ottimizzazione ha gia' prodotto il gemello .gz, nginx
# serve quello invece di ricomprimere ad ogni richiesta.
gzip_static         on;
gzip_types
    text/plain text/css text/xml text/javascript text/x-component text/calendar
    application/javascript application/x-javascript application/json
    application/xml application/xml+rss application/rss+xml application/atom+xml
    application/ld+json application/manifest+json application/vnd.ms-fontobject
    application/wasm application/x-font-ttf
    font/ttf font/otf image/svg+xml image/x-icon;
